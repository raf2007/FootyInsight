<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:padding="16dp">

    <!--   <ImageButton
         android:id="@+id/imageButton1"
         android:layout_width="wrap_content"
         android:layout_height="wrap_content"
         android:layout_centerHorizontal="true"
         android:layout_marginTop="50dp"
         android:background="@null"
         android:contentDescription="@string/img_button1_description"
         android:src="@drawable/ball1" />

    <ImageButton
        android:id="@+id/imageButton2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/imageButton1"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@null"
        android:contentDescription="@string/img_button2_description"
        android:src="@drawable/img2" />
    -->
    <!-- Add more ImageButtons as needed -->
    <Button
        android:layout_marginTop="50dp"
        android:id="@+id/button_open_easy_level"
        android:layout_width="match_parent"
        android:background="@color/green"
        android:textSize="25dp"
        android:layout_height="wrap_content"
        android:text="Easy"
        android:layout_gravity="center"/>

</RelativeLayout>
